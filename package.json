{
  "name": "rowelltz_tg_bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node dist/bot.js",
    "build": "tsc",
    "lint": "eslint . --ext .js,.ts",
    "dev": "ts-node-dev --respawn --transpile-only --poll --notify=false --clear --watch src src/bot.ts",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "docker:prod": "docker-compose up -d --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@telegraf/session": "^2.0.0-beta.7",
    "dotenv": "^17.2.1",
    "redis": "^4.7.1",
    "telegraf": "^4.16.3"
  }
}
